<!DOCTYPE html>
<html>
  <head>
    <title>３日間チャレンジ投稿</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f2f2f2;
        padding: 20px;
        margin-top: 100px;
      }

      .container {
        max-width: 600px;
        margin: 0 auto;
        background-color: #fff;
        border-radius: 5px;
        padding: 20px;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      }

      .form-group {
        margin-bottom: 20px;
      }

      .form-group label {
        font-weight: bold;
        display: block;
        margin-bottom: 5px;
      }

      .form-group input[type="text"],
      .form-group textarea {
        width: 100%;
        padding: 10px;
        border: 1px solid #ccc;
        border-radius: 5px;
        font-size: 14px;
      }

      .form-group select {
        width: 100%;
        padding: 10px;
        border: 1px solid #ccc;
        border-radius: 5px;
        font-size: 14px;
      }

      .form-group input[type="file"] {
        display: none;
      }

      .form-group .upload-button {
        display: inline-block;
        padding: 10px 20px;
        background-color: #3498db;
        color: #fff;
        border: none;
        border-radius: 5px;
        font-size: 14px;
        cursor: pointer;
      }

      .form-group .upload-button:hover {
        background-color: #2980b9;
      }

      .form-group .uploaded-image {
        margin-top: 10px;
        display: none;
      }

      .form-group .uploaded-image img {
        max-width: 200px;
        border-radius: 5px;
      }

      .form-group .submit-button {
        display: block;
        width: 100%;
        padding: 10px;
        background-color: #2ecc71;
        color: #fff;
        border: none;
        border-radius: 5px;
        font-size: 16px;
        cursor: pointer;
      }

      .form-group .submit-button:hover {
        background-color: #27ae60;
      }

      .delete-button {
        background-color: red;
        color: #fff;
        font-size: 14px;
        padding: 5px 10px;
        border-radius: 5px;
        display: block;
        margin-bottom: 5px;
        text-align: center;
      }

    </style>
  </head>
  <body>
    <div class="container">
      <h1>３日間チャレンジ投稿</h1>
      <br>
      <%= form_with(model: @goal, url: goal_path(@goal), method: :patch, html: { multipart: true }) do |f| %>
        <div class="form-group">
          <%= f.label :"Micca!ゴール" %>
          <%= f.text_field :title, required: true %>
        </div>

        <div class="form-group">
          <%= f.label :"詳細(計画は何度でも立て直そう！)" %>
          <%= f.text_area :description, rows: 5, required: true %>
        </div>



        <div class="form-group">
          <%= f.submit '更新する', class: 'submit-button' %>
          <%= link_to '削除（今からでも少しやってみるっていう手もあるよ！）', goal_path(@goal.id), method: :delete, data: { turbo_method: :delete }, class: 'delete-button' %>
        </div>
      <% end %>

    </div>
  </body>
</html>
